# CMakeLists.txt — Creep C++ solver (placeholder)
# Requires SUNDIALS 7.1.1+ (ARKODE integrator for stiff/non-stiff creep ODEs)
# Adapt from Fluor_Zr/Creep/cpp_utils/CMakeLists.txt

cmake_minimum_required(VERSION 3.16)
project(CreepSolver)

message(FATAL_ERROR "Creep CMakeLists.txt is a placeholder — configure SUNDIALS path and source files")
